name: Smart Pipeline

on:
  push:

jobs:
  lint:
    runs-on: ubuntu-latest

    steps:
      - name: Lint
        run: echo "Running lint"

  test:
    runs-on: ubuntu-latest

    steps:
      - name: Test
        run: echo "Running tests"